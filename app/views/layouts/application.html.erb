<!DOCTYPE html>
<html class="no-js" lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "Untitled" %></title>

    <%= stylesheet_link_tag    "application", media: "all" %>
    <%= javascript_include_tag "application", 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <header>
      <nav class="top-bar">
        <div class="top-bar-left">
          <h2><a href="#">NO MOO</a></h2>
          <p>dairy-free recipes just for you</p>
        </div>

        <div class="top-bar-right search-bar">
          <%= form_tag(search_path, method: :get) do %>
            <div><%= label_tag "Search for recipes:" %></div>
            <div><%= text_field_tag :q %></div>
            <div><%= submit_tag "Search", :name => nil, class: "button" %></div>
          <% end %>
        </div>
      </nav>

      <section class='flash'>
        <% flash.each do |name, message| %>
        <div class="<%= name %>"><%= message %></div>
        <% end %>
      </section>
    </header>

    <main>
      <%= yield %>
    </main>

    <footer class="footer-bar">
      <div>
        <p class="text-center">powered by</p>
      </div>
      <div class="small-2 small-centered columns">
        <a href="https://developer.edamam.com/"><%= image_tag "logo.jpg" %></a>
      </div>
    </footer>
  </body>
</html>
